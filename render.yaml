services:
  - type: web
    name: air-force-gestion
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_VERSION
        value: 22.20.0
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        generateValue: true